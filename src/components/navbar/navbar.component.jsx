import React from "react";
import {NavBarContainer} from "./navbar.styles";

const NavBar = () => {

    return (
        <NavBarContainer>

        </NavBarContainer>
    );
};

export default NavBar;